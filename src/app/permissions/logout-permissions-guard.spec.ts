import { LogoutPermissionsGuard } from './logout-permissions-guard';

describe('LogoutPermissionsGuard', () => {
  it('should create an instance', () => {
    expect(new LogoutPermissionsGuard()).toBeTruthy();
  });
});
